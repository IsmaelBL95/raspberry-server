# 🚫 Bloquear iframes (protección clickjacking)
add_header X-Frame-Options "DENY" always;

# 🚫 Evitar que el navegador intente adivinar tipos de archivo
add_header X-Content-Type-Options "nosniff" always;

# 🔒 Política estricta de referers: nunca enviar cabecera Referer
add_header Referrer-Policy "no-referrer" always;

# 🔒 Content Security Policy (CSP)
# Solo permite cargar recursos desde el mismo origen.
# Ojo: esto puede bloquear scripts, fuentes o imágenes externas.
add_header Content-Security-Policy "default-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'self';" always;

# 📜 Extra: Protege contra XSS reflejados (algunos navegadores antiguos)
add_header X-XSS-Protection "1; mode=block" always;